---
title: ZZ-组织架构图交接文档（副本）
tags: [交接, 索引, HR, meta]
type: 自动化采集
date: 2026-08-07
---

> ⚠️ **本文件是 workspace 主文档的完整副本（非纯指针）**。主文档修改后须在**同一回合**同步更新本副本，避免内容过期。
> 主文档：[`组织架构图-交接文档.md`](file:///C%3A/Users/v_yitcai/WorkBuddy/20260728154244/%E4%BA%A4%E6%8E%A5%E6%96%87%E6%A1%A3/%E7%BB%84%E7%BB%87%E6%9E%B6%E6%9E%84%E5%9B%BE-%E4%BA%A4%E6%8E%A5%E6%96%87%E6%A1%A3.md) ｜ [在 Obsidian 打开](obsidian://open?path=C%3A%5CUsers%5Cv_yitcai%5CWorkBuddy%5C20260728154244%5C%E4%BA%A4%E6%8E%A5%E6%96%87%E6%A1%A3%5C%E7%BB%84%E7%BB%87%E6%9E%B6%E6%9E%84%E5%9B%BE-%E4%BA%A4%E6%8E%A5%E6%96%87%E6%A1%A3.md)

---
title: 安全产品二部&三部 组织架构图（可编辑HTML）交接文档
tags: [HR, 组织架构, 交接文档]
source: "C:/Users/v_yitcai/WorkBuddy/20260728154244/交接文档/组织架构图-交接文档.md"
---

# 交接文档：安全产品二部 & 三部 组织架构图（可编辑 HTML）

> 这是写给**完全没有上下文的新会话**看的。照着读就能接手，不用翻聊天记录。
> 最近更新：2026-08-05。项目主要文件都在 `c:\Users\v_yitcai\WorkBuddy\20260804183228\`。

---

## 1. 我们在做什么（项目定位）

给**安全产品二部 / 三部**做一张**可编辑、可导出**的组织架构图网页（HTML）。

- 发起人：y / 蔡依彤（v_yitcai），云技服 HRBP+PM，负责把 HR 流程产品化。
- 用途：她要在图上**手填各级负责人**，然后截图 / 导 PDF 拿去汇报或发群，**不想碰代码**。
- 形态：一个自包含 HTML 文件，双击就能在浏览器打开；所有文字可直接点开编辑；编辑内容能存本地、能导出图片/PDF。
- 数据来源：桌面上的 `副本部.xlsx`（组织人员名单）。

**一句话**：一个"打开就能改、改完能存能导出"的组织架构图，不是静态图、不是要部署的服务。

---

## 2. 完成了什么（现状）

| 模块 | 状态 | 说明 |
|---|---|---|
| 数据解析 | ✅ | `gen_org.py` 读 xlsx → 按 部门/中心/小组 三级建树，表头自动定位（含 部门/中心/小组 的那行），数据行在其后 |
| 生成产物 | ✅ | 输出自包含 `org_chart.html`（已内联 html2canvas@1.4.1 + jspdf@2.5.1，不依赖外网） |
| 结构渲染 | ✅ | 二部 6 中心 / 三部 8 中心 = **14 张中心卡**；按源数据非空小组名生成 **55 个小框（小组）** |
| 全字段可编辑 | ✅ | 部门名 / 中心名 / 小组名 / 各级负责人 全部 `contenteditable`，带 `data-key` 持久化到 localStorage（键 `v2`） |
| 保存为独立文件 | ✅ | 「💾保存文件」把当前填写内容写进 HTML 本身并下载成一份独立副本 |
| 布局 | ✅ | 中心卡**固定等宽** `flex:0 0 calc(33.333% - 7px)`（min-width:150px）；每部门单主色（二部蓝 #2563eb / 三部橙 #f97316），紧凑卡片式 |
| 空白小框自动删除 | ✅ | `cleanupChips()`：小组名+负责人都为空的小框自动移除（加载时一次 + 每次失焦后一次） |
| 导出 | ✅ | 工具栏「导出图片」「导出PDF」「清空」按钮齐备 |
| 反复打磨 | ✅ | 经历多轮迭代（配色收敛、去等宽误读、回滚宽度自控件、加空白删框），当前为稳定版 |

**当前唯一真源**：`gen_org.py`（生成器）+ `org_chart.html`（产物）。改结构/数据 → 改 `gen_org.py` 后重跑；改样式/交互 → 改 `gen_org.py` 里的 `TEMPLATE` 后重跑。

---

## 3. 文件清单（别认错文件）

| 文件 | 角色 | 备注 |
|---|---|---|
| `gen_org.py` | **生成器（核心）** | 改这个就行。只用 `openpyxl`，无 pandas 依赖 |
| `org_chart.html` | **当前产物（唯一真源）** | 双击打开即用；带全部交互逻辑 |
| `org_chart_v2.html` / `org_chart_2036.html` / `org_chart_2042.html` | 历史实验产物 | ⚠️ **已被 `org_chart.html` 取代，勿当作当前版本** |
| `check*.py` / `verify.py` / `read_xlsx.py` / `check_body.py` / `check_width*.py` | 一次性校验脚本 | 正常重生成本不需要，纯排错用 |
| `org_list.txt` / `org_struct.txt` / `xlsx_out.txt` | 数据 dump | 排错参考，非真源 |
| `html2canvas.min.js` / `jspdf.umd.min.js` | 库源文件 | 已被内联进 `org_chart.html`；开发期用来内联，产物不依赖它们 |

---

## 4. 怎么重新生成 / 怎么预览

### 重新生成（数据或结构改完后）
```powershell
cd c:\Users\v_yitcai\WorkBuddy\20260804183228
C:\Users\v_yitcai\AppData\Local\Programs\Python\Python312\python.exe gen_org.py
```
> ⚠️ `python` 不在 PATH，必须用上面完整路径。只需 `openpyxl` 可用（已验证）。

### 本地预览（必须在 http 服务下，别用 file:// 直接开导出可能受限）
```powershell
# 1) 先杀掉占用端口的旧 http.server（否则看到旧内容/404）
Get-CimInstance Win32_Process -Filter "Name='python.exe'" | Where-Object { $_.CommandLine -like '*http.server*' } | ForEach-Object { Stop-Process -Id $_.ProcessId -Force }

# 2) 起新服务（端口挑个没被占的，历史用过 8765→8899→8901→8910）
C:\Users\v_yitcai\AppData\Local\Programs\Python\Python312\python.exe -m http.server 8910 --directory c:\Users\v_yitcai\WorkBuddy\20260804183228

# 3) 浏览器开
# http://localhost:8910/org_chart.html
```

---

## 5. 之前卡在哪 + 解决方案

### 卡点 A：数据源字段口径冲突
- **现象**：源 xlsx 的「中心」字段里出现了 `业务管理中心`（属二部），以及唯一的「中心&小组」串列值 `产品管理组`。
- **冲突**：纯按字段渲染会多出用户不要的节点；`产品管理组` 既是中心又像小组，归属模糊。
- **解决方案（用户拍板）**：
  - `业务管理中心` 整节点剔除（代码 `DROP_CENTERS = {'业务管理中心'}`，连下属小组一并移除）；
  - `产品管理组` 保留为**二部 + 三部共用的中心**（它是唯一串列值，按用户口径当作中心处理）。
- **结论**：**用户权威口径 > 源字段原始值**。不要自作主张改回。

### 卡点 B：布局"等宽"被反复误读
- **现象**：用户要"等宽卡片"，我第一版误读成强制 Grid 等宽（错）；后来又加了「宽[N]px」自调控件（170/280 可变宽）。
- **冲突**：用户其实**不要任何调宽 UI**，就要固定等宽、干净。
- **解决方案**：中心卡统一 `flex:0 0 calc(33.333% - 7px)` 固定等宽，删掉所有调宽按钮/输入框/持久化。**用户于 2026-08-04 明确回滚了宽度自控件。**

### 卡点 C：空白小框处理
- **现象**：数据层不会产空白框（解析只加非空小组名），但用户要求"小框内没文字就自动删"。
- **解决方案**：加 `cleanupChips()` 在运行时处理（加载一次 + 失焦后一次），只删"小组名+负责人都空"的框；负责人槽的"填写人"是 CSS 占位伪元素，不计入文字，不会误删有小组名但没填负责人的框。

---

## 6. 踩过的坑（不要再踩 ⚠️）

1. **宽度自控件已死，别复活。** 用户明确回滚了「宽[N]px」输入框 + ⚙️宽按钮 + 170/280 可变宽。任何"让用户可以调卡片宽度"的念头都不要落地。布局就用固定等宽。
2. **预览端口被旧进程占 → 看到旧内容/404。** 每次重改后必须**先杀旧 http.server 再起重起**，端口递进（8765→8899→8901→8910）。不然你会以为改动没生效，其实是开着旧服务。
3. **「💾保存文件」下载的是独立副本**（默认 `Downloads/组织架构图.html`），**不会随重新生成自动更新**。用户说"怎么没更新"几乎都是打开了那份旧下载。正确做法：双击 workspace 里的 `org_chart.html` 源文件，或重新点保存覆盖下载。
4. **GitHub Pages 中文文件名会 404。** 以后若要公开托管，必须用英文名；且**公开动作涉及内部组织结构数据，推送前必须用户明确同意**。
5. **改动要验证闭环。** 用户要的是 grep / 实际生成校验，不是"我说改了"。每次改完实际跑一下、核对数量或关键词（例：宽度控件残留应为 0 匹配；中心卡应为 14、chip 应为 55）。
6. **数据口径优先级**：用户口径 > 源字段。见卡点 A。
7. **别碰旧产物文件**（`org_chart_v2/2036/2042.html`），它们已被取代。
8. **python 用完整路径**，仅依赖 `openpyxl`。

---

## 7. 用户协作偏好 / 边界（务必遵守）

- **极简命令驱动**：常用"要""改了吗"这类确认；厌恶冗长解释与寒暄。结论先行。
- **严谨**：要求"严格按中心/小组字段"，发现数据异常（中心漏渲染、串列）会直接指出。
- **审美**：要"活泼卡片式"，嫌颜色多/卡片松，倾向收敛配色 + 紧凑。
- **保留人工所有权**：活动主题、负责人姓名等可编辑字段由用户自己填，**不要 AI 自动替他写死**。
- **对外动作谨慎**：发消息 / 公开推送等外部操作先问；内部读、整理、学大胆。
- **验证闭环**：同上第 5 条。

---

## 8. 可能的下一步（待用户确认，不要擅自开干）

- [ ] 视觉/布局细节微调（间距、字号、配色、卡片密度）
- [ ] 导出 PDF/图片确认实际渲染效果
- [ ] 推送 GitHub Pages（需先确认仓库 + 英文名 + 用户授权）
- [ ] 把这份交接文档同步进 `.workbuddy/memory/MEMORY.md` 作为长期真源
- [ ] 其它用户临时提出的需求

---

### 快速接手三句话
1. 改结构/数据/样式 → 编辑 `gen_org.py` 的解析或 `TEMPLATE` → 用完整路径 python 重跑。
2. 预览 → 杀旧 http.server、起重服务、开 `http://localhost:8910/org_chart.html`。
3. 任何"调宽卡片""自作主张改数据口径""推 GitHub 不确认"都是坑，别踩。
