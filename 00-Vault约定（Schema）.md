---
title: Vault 约定（Schema）— 单一 vault 治理规范
type: meta
tags: [meta, 索引, MOC]
created: 2026-08-13
updated: 2026-08-13
note: 锁定目录 / frontmatter / 受控 tag / 命名模板，作为所有写入方（手工 + 知识采集自动化 + skill 导出）的唯一对齐基准
---

# 00 · Vault 约定（Schema）

> 本 vault 已合并为**单一 vault**（根 = `活动`）。此文档是所有内容写入的**唯一对齐基准**，新增内容（不论手工还是自动化）必须遵循，避免再次出现"四方写入、零统一管理"导致的乱。

## 一、目录结构（按主题分区，不按工具）

| 区 | 路径 | 内容性质 | 检索前缀 |
|---|---|---|---|
| 活动原生知识 | `素材库/` `案例库/` `项目总览/` `文化活动/` `手册/` `交接文档/` | 业务知识 / 策划 / 项目追踪 / 手册 / 交接 | 各自前缀 |
| 知识采集库 | `知识采集库/` | 自动采集沉淀（run 卡 + 汇总 + 索引） | `知识采集库/` |
| 技能库 | `技能库/` | WorkBuddy skill 导出（**工具文档，非业务知识**） | `技能库/` |
| 简历 & 模板 | `简历/` `Templates/` | 个人简历 / 笔记模板 | — |
| 入口 | `00-知识库总览.md` | 顶层 MOC | — |

**铁律**：内容按"主题/用途"落区，**不再按来源工具分目录**；**项目总览 / 方案 / 手册 / 交接类笔记一律进对应主题文件夹，根目录只留 `00-*` 入口与 MOC 文档**；跨区引用用 `[[笔记名]]` 或 `[[区/笔记名]]`，禁止 `..` 上级路径。

## 二、Frontmatter 字段词典（受控）

| 字段 | 必填 | 取值规范 | 说明 |
|---|---|---|---|
| `title` | ✅ | 中文语义名 | 笔记标题 |
| `tags` | ✅ | 仅用第三节受控词表 | 多值用 YAML 列表 `[a, b]` |
| `source` | 采集必填 | 来源说明（出处透明） | 手工笔记可省 |
| `date` | 采集必填 | `YYYY-MM-DD` | 采集日期 |
| `quality` | 采集必填 | 1–5 整数 | 六维评估分 |
| `type` | 采集必填 | `自动化采集` / `MOC` / `meta` / `模板` | 内容类型分层 |
| `updated` | 建议 | `YYYY-MM-DD` | 末次更新 |
| `created` | 建议 | `YYYY-MM-DD` | 创建日期 |

**禁止**：把颜色值（如 `#2563eb`）、代码注释、序号（`#2`）写进 `tags`；正文里的 `#xxx` 一律用反引号 `` `#xxx` `` 包裹，避免被 Obsidian 当 tag。

## 三、受控 Tag 词表（含同义归一映射）

下列为**唯一合法 tag**。历史同义词已归一，新增禁止再分裂：

**活动类型（中文 canonical）**
- `员工大会`（← 已合并 staff-meeting / townhall / all-hands / 全员大会）
- `offsite`（管理层闭门，保留英文术语）
- `颁奖`（← award / 颁奖典礼）
- `开放日`（← openday / Open / Day / Open Day）
- `破冰`（← icebreaker）
- `下午茶`（← afternoontea / 下午茶研讨）
- `AI漫剧` `AI沙龙` `文化活动` `活动/ai` `活动/方法` `活动应用`

**治理 / 工具**
- `知识采集` `自动化采集` `索引` `MOC` `meta` `导航` `素材库` `技能库` `模板` `交接` `SOP` `复盘` `项目总览`

> 同义归一规则：同概念只留一个 canonical；英文 slug 仅作**文件夹名**（`知识采集库/素材/<slug>/`），不作 tag。

## 四、命名模板（受控）

| 类型 | 模板 | 示例 |
|---|---|---|
| 采集 run 卡 | `<主题>-<YYYYMMDD>-第N轮-知识卡.md` | `员工大会-2026-08-13-第十二轮-知识卡.md` |
| 采集汇总 | `<主题>-知识卡汇总.md` | `员工大会-知识卡汇总.md` |
| 项目总览 | `<项目名>总览.md` | `AI文化氛围建设项目总览.md` |
| 顶层入口 | `00-*.md` | `00-知识库总览.md` |

**统一用语**：批次一律用「**轮**」（第N轮），不再混用「批/轮」；slug 用小写英文，中文主题放文件名主体。

## 五、内容类型分层（检索视图）

1. **知识笔记**（业务知识，图谱核心）
2. **自动化采集**（run 卡 + 汇总，带 `type: 自动化采集`）
3. **索引 / MOC**（`type: MOC`）
4. **模板**（`type: 模板`）
5. **工具文档**（技能库，检索时 `技能库/` 前缀过滤，不污染业务图谱）

## 六、写入方约束（防回归）

- **知识采集自动化**：落库路径固定 `活动/知识采集库/素材/<slug>/runs/` 与 `活动/知识采集库/素材/<slug>/<主题>-知识卡汇总.md`；链接一律 `[[知识采集库/素材/...]]`；**不再写回 vault 外旧位置**。
- **手工笔记**：归入对应区，补全 frontmatter + 受控 tag。
- **skill 导出**：仅进 `技能库/`，正文 `#xxx` 颜色/序号加反引号。
- **单一 vault**：根 `活动/.obsidian` 为唯一配置；不在子目录再建 `.obsidian`。
